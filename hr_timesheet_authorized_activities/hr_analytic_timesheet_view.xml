<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>
        <record id="hr_timesheet_line_tree" model="ir.ui.view">
            <field name="name">hr.analytic.timesheet.tree</field>
            <field name="model">hr.analytic.timesheet</field>
            <field name="inherit_id" ref="hr_activity_on_timesheet.hr_timesheet_line_tree"/>
            <field name="arch" type="xml">
                <!-- Add activity_id argument to on_change_account_id -->
                <field name="account_id" position="replace">
                    <field
                        domain="[('type','in',['normal','contract']),('state', '&lt;&gt;', 'close'),('use_timesheets','=',1)]"
                        name="account_id"
                        on_change="on_change_account_id(account_id, user_id, activity_id)"
                        context="{'default_use_timesheets': 1}"
                    />
                </field>
            </field>
        </record>

        <record id="hr_timesheet_line_form" model="ir.ui.view">
            <field name="name">hr.analytic.timesheet.form</field>
            <field name="model">hr.analytic.timesheet</field>
            <field name="inherit_id" ref="hr_activity_on_timesheet.hr_timesheet_line_form"/>
            <field name="arch" type="xml">
                <!-- Add activity_id argument to on_change_account_id -->
                <field name="account_id" position="replace">
                    <field
                        domain="[('type','in',['normal','contract']),('state', '&lt;&gt;', 'close'),('use_timesheets','=',1)]"
                        name="account_id"
                        on_change="on_change_account_id(account_id, user_id, activity_id)"
                        context="{'default_use_timesheets': 1}"
                    />
                </field>
            </field>
        </record>

    </data>
</openerp>