<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <t
        t-name="hr_governance.FormWrapperView"
        t-inherit="web.FormView"
        t-inherit-mode="primary"
    >
        <xpath
            expr="//t[@t-set-slot='control-panel-additional-actions']"
            position="replace"
        >
            <t t-set-slot="control-panel-additional-actions">
                <t t-if="canEdit">
                    <CogMenu
                        getActiveIds="() => model.root.isNew ? [] : [model.root.resId]"
                        context="props.context"
                        items="props.info.actionMenus ? this.actionMenuItems : {}"
                        isDomainSelected="model.root.isDomainSelected"
                        resModel="model.root.resModel"
                        domain="props.domain"
                        onActionExecuted="() => model.load({ resId: model.root.resId, resIds: model.root.resIds })"
                        shouldExecuteAction.bind="shouldExecuteAction"
                    />
                </t>
            </t>
        </xpath>
        <xpath
            expr="//t[@t-set-slot='control-panel-create-button'][2]"
            position="replace"
        >
            <t t-set-slot="control-panel-create-button">
                <t t-if="canCreate">
                    <button
                        type="button"
                        class="btn btn-outline-primary o_form_button_create"
                        data-hotkey="c"
                        t-att-data-type="'circle'"
                        t-on-click.stop="create"
                    >Create Circle</button>
                    <button
                        type="button"
                        class="btn btn-outline-primary o_form_button_create"
                        data-hotkey="c"
                        t-att-data-type="'role'"
                        t-on-click.stop="create"
                    >Create Role</button>
                </t>
            </t>
        </xpath>
    </t>
</templates>
