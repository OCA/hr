<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2024 ACSONE SA/NV
     License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl). -->
<odoo>
    <record id="timesheet_prefill_form_view" model="ir.ui.view">
        <field name="name">timesheet.prefill.form (in hr_timesheet_prefill)</field>
        <field name="model">timesheet.prefill</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <group>
                        <field
                            name="employee_ids"
                            widget="many2many_tags"
                            options="{'no_create': True}"
                        />
                        <field
                            name="project_ids"
                            widget="many2many_tags"
                            options="{'no_create': True}"
                        />
                    </group>
                    <group>
                        <field name="date_from" />
                        <field name="date_to" />
                    </group>
                </group>
                <footer>
                    <button
                        name="action_generate"
                        string="Generate"
                        type="object"
                        class="btn-primary"
                        confirm="Do you really want to generate empty timesheet now?"
                    />
                    <button string="Cancel" class="btn-default" special="cancel" />
                </footer>
            </form>
        </field>
    </record>

    <record id="timesheet_prefill_action" model="ir.actions.act_window">
        <field name="name">Timesheet prefill</field>
        <field name="res_model">timesheet.prefill</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="view_id" ref="timesheet_prefill_form_view" />
    </record>

    <menuitem
        id="timesheet_prefill_menu"
        parent="hr_timesheet.menu_hr_time_tracking"
        sequence="20"
        action="timesheet_prefill_action"
        name="Timesheet prefill"
    />
</odoo>
