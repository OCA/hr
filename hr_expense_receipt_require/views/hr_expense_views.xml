<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <record id="product_product_expense_form_view" model="ir.ui.view">
            <field name="name">product.product.expense.form</field>
            <field name="model">product.product</field>
            <field name="inherit_id" ref="hr_expense.product_product_expense_form_view"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='standard_price']" position="after">
                    <field name="expense_receipt_required"/>
                </xpath>
            </field>
        </record>

        <record id="hr_expense.hr_expense_submit_action_server" model="ir.actions.server">
            <field name="name">Submit Report</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="hr_expense.model_hr_expense"/>
            <field name="binding_model_id" ref="hr_expense.model_hr_expense"/>
            <field name="state">code</field>
            <field name="code">
if records:
    action = records.with_context({'select_multi_records': True}).action_submit_expenses()
            </field>
        </record>
</odoo>
