* Go to `Expenses > Configuration > Expense Types` and create different
  expense types. Now you can select these expense types in expense form.
* Or directly, create an expense type from expense form.
